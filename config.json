{
    "__": "NOTE : keys starting with underscores are comments. t2m ignores them.  We are doing this because json doesn't officially support comments",

    "_db_path_": "DB path.  This is the sqlite3 database for this server.  Keep in mind that this should not be accessible from the web server.",
    "db_path":"../../t2m.db",

    "_base_url_": "Base URL facing the external world.",
    "base_url":"http://localhost:8000/"
}
